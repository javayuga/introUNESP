#include<stdlib.h>
#include<stdio.h> 

int main()
{
  while (/*condicao*/){
  
  /*  caso a condição seja verdadeira, 
   essas instruções serão executadas */
  }
  
  system("PAUSE");
  return 0;   
    
}

/* exemplo:

int main()
{
    int i=0, x=0;
    
    while(i<3){
      scanf("%d", &x);
      printf("%d\n", x);
      i++;
    }
    
    i é a variável contadora inicializada em 0
    x é variável que vai armazenar o numero inteiro
    
    enquanto i for menor que 3, o programa irá requisitar a entrada
     do numero inteiro ao usuário com scanf, seguido da impressao desse
     mesemo numero com printf
    
    incremento de 1 na variável i ao fim da execução das instruções
    
    system("PAUSE");
    return 0;
}
*/
